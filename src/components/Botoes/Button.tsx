import { StyleSheet } from 'react-native';

export const styleButton = StyleSheet.create({
      button: {
        color: 'white',
        textAlign: 'center',
        fontWeight: 'bold',
        padding: 15,
      },
})