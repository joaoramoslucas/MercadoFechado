import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
      button: {
        color: 'white',
        textAlign: 'center',
        fontWeight: 'bold',
        padding: 15,
      },
})